name: myapp
description: "Deploy myapp to local"

source:
  strategy: docker_full
  host: local
  app: myapp
  version: "0.1.0"

target:
  strategy: docker_full
  host: local
  app: myapp
  version: "0.1.0"
  remote_dir: ~/myapp
  domain: myapp.example.com
  verify_url: http://localhost:8000/health
  env_file: .env
